import { CreateAccountUseCase } from '../../../src/application/use-cases/create-account-use-case'

// describe('CreateAccountUseCase', () => {
//   let createAccountUseCase: CreateAccountUseCase

//   beforeEach(() => {
//     createAccountUseCase = new CreateAccountUseCase()
//   })

// it('should create a valid user account', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// it('should create a valid driver account', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// it('should display an error message if the account already exists', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// it('should display error message if the account is not created', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// it('should not create an account with invalid email', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// it('should not create an account with invalid cpf', async () => {
//   expect(await createAccountUseCase.execute({} as any)).toBe(true)
// })
// })
it('should pass', () => {
  expect(1 + 1).toBe(2)
})